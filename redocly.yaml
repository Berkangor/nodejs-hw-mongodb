# redocly.yaml
apis:
  sample@v1:
    root: docs/openapi.yaml

extends:
  - recommended

rules:
  no-unused-components: error

# Bunlar UI (preview/build-docs) tarafına ait ayarlar
referenceDocs:
  htmlTemplate: ./docs/index.html
  theme:
    colors:
      primary:
        main: '#32329f'
  generateCodeSamples:
    languages:
      - lang: curl
      - lang: Node.js
      - lang: JavaScript
